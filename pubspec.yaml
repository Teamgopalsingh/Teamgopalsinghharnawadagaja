name: team_gopalsingh_app
description: "टीम गोपालसिंह - हरनावदा गजा (Team GopalSingh - Harnawada Gaja) Production Mobile App"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  provider: ^6.1.1
  flutter_bloc: ^8.1.3
  supabase_flutter: ^2.3.4
  url_launcher: ^6.2.4
  google_fonts: ^6.1.0
  image_picker: ^1.0.7
  geolocator: ^10.1.0
  flutter_local_notifications: ^17.0.0
  intl: ^0.19.0
  cached_network_image: ^3.3.1
  share_plus: ^7.2.2
  pdf: ^3.10.8
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
